name: 🤖 Auto Version & Discord Notify

on:
  push:
    branches:
      - main
  workflow_dispatch:  # permite execução manual também

permissions:
  contents: write  # necessário para criar tags e atualizar arquivos

jobs:
  auto_version:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Clona o repositório
      - name: Checkout do repositório
        uses: actions/checkout@v4

      # 2️⃣ Detecta se o commit contém número de versão (ex: v2.4.0)
      - name: Detectar número de versão no commit
        id: version
        run: |
          COMMIT_MSG="${{ github.event.head_commit.message }}"
          echo "🧩 Mensagem do commit: $COMMIT_MSG"
          if [[ "$COMMIT_MSG" =~ v[0-9]+\.[0-9]+\.[0-9]+ ]]; then
            VERSION=$(echo "$COMMIT_MSG" | grep -oE "v[0-9]+\.[0-9]+\.[0-9]+")
            echo "✅ Versão detectada: $VERSION"
            echo "VERSION=$VERSION" >> $GITHUB_ENV
          else
            echo "⚠️ Nenhuma versão detectada — commit normal."
            echo "SKIP=true" >> $GITHUB_ENV
          fi

      # 3️⃣ Atualiza o badge de versão no README.md
      - name: Atualizar badge de versão no README
        if: env.SKIP != 'true'
        run: |
          echo "🪄 Atualizando badge de versão no README.md..."
          sed -i -E "s/Version-[0-9]+\.[0-9]+\.[0-9]+/Version-${VERSION}/" readme.md
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add readme.md
          git commit -m "Atualiza badge de versão para ${VERSION}" || echo "🟡 Nenhuma mudança no badge."
          git push || true

      # 4️⃣ Cria e envia a tag automaticamente
      - name: Criar tag automática
        if: env.SKIP != 'true'
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git tag $VERSION
          git push origin $VERSION
          echo "🏷️ Tag $VERSION criada e enviada."

      # 5️⃣ Lê o último changelog
      - name: Obter changelog recente
        if: env.SKIP != 'true'
        run: |
          LATEST_CHANGELOG=$(awk '/^## /{if (NR!=1) exit} {print}' changelog.md)
          echo "LATEST_CHANGELOG<<EOF" >> $GITHUB_ENV
          echo "$LATEST_CHANGELOG" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV
          echo "📜 Último changelog preparado."

      # 6️⃣ Envia mensagem formatada para o Discord
      - name: Enviar notificação ao Discord
        if: env.SKIP != 'true'
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        with:
          args: |
            🎯 **Nova versão `${{ env.VERSION }}` publicada automaticamente!**
            🧙 **Repositório:** [`${{ github.repository }}`](https://github.com/${{ github.repository }})
            👤 **Autor:** `${{ github.actor }}`
            🕐 **Data:** `${{ github.event.head_commit.timestamp }}`
            📝 **Changelog:**
            ${{ env.LATEST_CHANGELOG }}

      # 7️⃣ Confirmação no log da Action
      - name: Confirmar envio
        if: env.SKIP != 'true'
        run: echo "✅ Versão $VERSION publicada, badge atualizado e notificação enviada!"
