name: 🤖 Auto Version & Discord Notify (Lyra Final v2)

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write  # necessário para criar tags e atualizar o README

jobs:
  auto_version:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Clona o repositório
      - name: Checkout do repositório
        uses: actions/checkout@v4

      # 2️⃣ Detecta número de versão no commit (ex: v2.4.0 ou v2.4.0!)
      - name: Detectar número de versão no commit
        id: version
        run: |
          COMMIT_MSG="${{ github.event.head_commit.message }}"
          echo "🧩 Mensagem do commit: $COMMIT_MSG"
          if [[ "$COMMIT_MSG" =~ v[0-9]+\.[0-9]+\.[0-9]+ ]]; then
            VERSION=$(echo "$COMMIT_MSG" | grep -oE "v[0-9]+\.[0-9]+\.[0-9]+")
            echo "✅ Versão detectada: $VERSION"
            echo "VERSION=$VERSION" >> $GITHUB_ENV
          else
            echo "⚠️ Nenhuma versão detectada — commit normal."
            echo "SKIP=true" >> $GITHUB_ENV
          fi

      # 3️⃣ Atualiza badge de versão no README.md
      - name: Atualizar badge de versão no README
        if: env.SKIP != 'true'
        run: |
          echo "🪄 Atualizando badge de versão no README.md..."
          sed -i -E "s/Version-[0-9]+\.[0-9]+\.[0-9]+/Version-${VERSION}/" readme.md
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add readme.md
          git commit -m "Atualiza badge de versão para ${VERSION}" || echo "🟡 Nenhuma mudança detectada."
          git push || true

      # 4️⃣ Cria e envia a tag automaticamente (modo híbrido)
      - name: Criar tag automática (modo híbrido)
        if: env.SKIP != 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"

          COMMIT_MSG="${{ github.event.head_commit.message }}"
          FORCE_TAG=false

          # Detecta se o commit contém um "!" após a versão (ex: v2.5.0!)
          if [[ "$COMMIT_MSG" =~ v[0-9]+\.[0-9]+\.[0-9]+! ]]; then
            FORCE_TAG=true
            VERSION=$(echo "$COMMIT_MSG" | grep -oE "v[0-9]+\.[0-9]+\.[0-9]+")
            echo "⚡ Modo forçado ativado — tag ${VERSION} será substituída."
          fi

          # Verifica se a tag existe no repositório remoto
          if git ls-remote --tags origin | grep -q "refs/tags/$VERSION$"; then
            if [ "$FORCE_TAG" = true ]; then
              git tag -f $VERSION
              git push -f https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git $VERSION
              echo "🏷️ Tag $VERSION recriada (forçada)."
            else
              echo "⚠️ A tag $VERSION já existe. Pulando criação (modo normal)."
              exit 0
            fi
          else
            git tag $VERSION
            git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git $VERSION
            echo "🏷️ Tag $VERSION criada e enviada com sucesso!"
          fi

      # 5️⃣ Extrai SOMENTE o último changelog completo
      - name: Obter changelog recente
        if: env.SKIP != 'true'
        run: |
          echo "📖 Extraindo último changelog completo..."
          # Captura bloco entre o primeiro "## " e o próximo (ou fim do arquivo)
          LATEST_CHANGELOG=$(awk '
            /^## / {
              if (found) exit; 
              found=1; next
            }
            found
          ' changelog.md)

          # Remove espaços extras e linhas vazias iniciais
          LATEST_CHANGELOG=$(echo "$LATEST_CHANGELOG" | sed '/^[[:space:]]*$/d')

          if [ -z "$LATEST_CHANGELOG" ]; then
            echo "⚠️ Nenhum changelog encontrado. Usando fallback."
            LATEST_CHANGELOG="Nenhum changelog disponível nesta versão."
          fi

          echo "LATEST_CHANGELOG<<EOF" >> $GITHUB_ENV
          echo "$LATEST_CHANGELOG" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV
          echo "📜 Último changelog preparado (somente o mais recente)."

      # 6️⃣ Envia mensagem formatada (Markdown estilizado para Discord)
      - name: Enviar notificação ao Discord
        if: env.SKIP != 'true'
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        with:
          args: |
            🧙 **Nova versão `${{ env.VERSION }}` publicada automaticamente!**
            📦 **Repositório:** [${{ github.repository }}](https://github.com/${{ github.repository }})
            👤 **Autor:** ${{ github.actor }}
            🕐 **Data:** `${{ github.event.head_commit.timestamp }}`
            🪶 **Changelog:**
            ```md
            ${{ env.LATEST_CHANGELOG }}
            ```
            🔗 [Ver no GitHub](https://github.com/${{ github.repository }}/releases/tag/${{ env.VERSION }})
            🧩 *Lyra the Wise • Build automático do GitHub Actions*

      # 7️⃣ Confirmação final no log
      - name: Confirmar envio
        if: env.SKIP != 'true'
        run: echo "✅ Versão $VERSION publicada, tag criada (ou forçada) e notificação entregue!"
