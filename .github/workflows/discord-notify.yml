name: 🤖 Auto Version & Discord Notify (Lyra Styled)

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  auto_version:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Clona o repositório
      - name: Checkout do repositório
        uses: actions/checkout@v4

      # 2️⃣ Detecta número de versão no commit
      - name: Detectar número de versão no commit
        id: version
        run: |
          COMMIT_MSG="${{ github.event.head_commit.message }}"
          echo "🧩 Mensagem do commit: $COMMIT_MSG"
          if [[ "$COMMIT_MSG" =~ v[0-9]+\.[0-9]+\.[0-9]+ ]]; then
            VERSION=$(echo "$COMMIT_MSG" | grep -oE "v[0-9]+\.[0-9]+\.[0-9]+")
            echo "✅ Versão detectada: $VERSION"
            echo "VERSION=$VERSION" >> $GITHUB_ENV
          else
            echo "⚠️ Nenhuma versão detectada — commit normal."
            echo "SKIP=true" >> $GITHUB_ENV
          fi

      # 3️⃣ Atualiza badge de versão no README
      - name: Atualizar badge de versão no README
        if: env.SKIP != 'true'
        run: |
          echo "🪄 Atualizando badge de versão no README.md..."
          sed -i -E "s/Version-[0-9]+\.[0-9]+\.[0-9]+/Version-${VERSION}/" readme.md
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add readme.md
          git commit -m "Atualiza badge de versão para ${VERSION}" || echo "🟡 Nenhuma mudança detectada."
          git push || true

      # 4️⃣ Cria tag automaticamente (modo híbrido)
      - name: Criar tag automática (modo híbrido)
        if: env.SKIP != 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"

          COMMIT_MSG="${{ github.event.head_commit.message }}"
          FORCE_TAG=false

          if [[ "$COMMIT_MSG" =~ v[0-9]+\.[0-9]+\.[0-9]+! ]]; then
            FORCE_TAG=true
            VERSION=$(echo "$COMMIT_MSG" | grep -oE "v[0-9]+\.[0-9]+\.[0-9]+")
            echo "⚡ Modo forçado ativado — tag ${VERSION} será substituída."
          fi

          if git ls-remote --tags origin | grep -q "refs/tags/$VERSION$"; then
            if [ "$FORCE_TAG" = true ]; then
              git tag -f $VERSION
              git push -f https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git $VERSION
              echo "🏷️ Tag $VERSION recriada (forçada)."
            else
              echo "⚠️ A tag $VERSION já existe. Pulando criação (modo normal)."
              exit 0
            fi
          else
            git tag $VERSION
            git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git $VERSION
            echo "🏷️ Tag $VERSION criada e enviada com sucesso!"
          fi

      # 5️⃣ Extrai changelog completo e aplica truncamento elegante
      - name: Obter changelog mais recente
        if: env.SKIP != 'true'
        run: |
          LATEST_CHANGELOG=$(awk '
            /^## / {
              if (found) exit;
              found=1; next;
            }
            found
          ' changelog.md)

          if [ -z "$LATEST_CHANGELOG" ]; then
            echo "⚠️ Nenhum changelog encontrado. Usando fallback."
            LATEST_CHANGELOG="Nenhum changelog disponível nesta versão."
          fi

          # Truncamento elegante com aviso
          LENGTH=${#LATEST_CHANGELOG}
          if [ $LENGTH -gt 1500 ]; then
            LATEST_CHANGELOG="${LATEST_CHANGELOG:0:1500}\n\n... _(changelog truncado — veja o completo no GitHub)_"
          fi

          echo "LATEST_CHANGELOG<<EOF" >> $GITHUB_ENV
          echo "$LATEST_CHANGELOG" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV
          echo "📜 Último changelog preparado (com truncamento elegante)."

      # 6️⃣ Envia notificação com visual profissional (embed simulada)
      - name: Enviar notificação ao Discord
        if: env.SKIP != 'true'
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        with:
          args: |
            🧙 **Atualização detectada em [${{ github.repository }}](https://github.com/${{ github.repository }})**
            👤 **Autor:** `${{ github.actor }}`
            📝 **Commit:** `${{ github.event.head_commit.message }}`
            🌿 **Branch:** `${{ github.ref_name }}`
            🔗 [Ver commit](https://github.com/${{ github.repository }}/commit/${{ github.sha }})

            🗒️ **Últimas alterações:**
            ```md
            ${{ env.LATEST_CHANGELOG }}
            ```

            🧩 *Lyra the Wise • Build automático do GitHub Actions*

      # 7️⃣ Confirmação final
      - name: Confirmar envio
        if: env.SKIP != 'true'
        run: echo "✅ Versão $VERSION publicada e changelog enviado (com truncamento elegante)."
