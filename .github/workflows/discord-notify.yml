name: 🤖 Auto Version & Discord Notify (Lyra Style)

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write  # necessário para criar tags e atualizar o README

jobs:
  auto_version:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Clona o repositório
      - name: Checkout do repositório
        uses: actions/checkout@v4

      # 2️⃣ Detecta número de versão no commit (ex: v2.4.0)
      - name: Detectar número de versão no commit
        id: version
        run: |
          COMMIT_MSG="${{ github.event.head_commit.message }}"
          echo "🧩 Mensagem do commit: $COMMIT_MSG"
          if [[ "$COMMIT_MSG" =~ v[0-9]+\.[0-9]+\.[0-9]+ ]]; then
            VERSION=$(echo "$COMMIT_MSG" | grep -oE "v[0-9]+\.[0-9]+\.[0-9]+")
            echo "✅ Versão detectada: $VERSION"
            echo "VERSION=$VERSION" >> $GITHUB_ENV
          else
            echo "⚠️ Nenhuma versão detectada — commit normal."
            echo "SKIP=true" >> $GITHUB_ENV
          fi

      # 3️⃣ Atualiza badge de versão no README.md
      - name: Atualizar badge de versão no README
        if: env.SKIP != 'true'
        run: |
          echo "🪄 Atualizando badge de versão no README.md..."
          sed -i -E "s/Version-[0-9]+\.[0-9]+\.[0-9]+/Version-${VERSION}/" readme.md
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add readme.md
          git commit -m "Atualiza badge de versão para ${VERSION}" || echo "🟡 Nenhuma mudança detectada."
          git push || true

      # 4️⃣ Cria e envia a tag automaticamente (com autenticação segura)
      - name: Criar tag automática
        if: env.SKIP != 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git tag $VERSION
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git $VERSION
          echo "🏷️ Tag $VERSION criada e enviada com sucesso!"

      # 5️⃣ Lê o último changelog com fallback automático
      - name: Obter changelog recente
        if: env.SKIP != 'true'
        run: |
          LATEST_CHANGELOG=$(awk '/^## /{if (NR!=1) exit} {print}' changelog.md)
          if [ -z "$LATEST_CHANGELOG" ]; then
            echo "⚠️ Nenhum changelog encontrado. Usando fallback."
            LATEST_CHANGELOG="Nenhum changelog disponível nesta versão."
          fi
          echo "LATEST_CHANGELOG<<EOF" >> $GITHUB_ENV
          echo "$LATEST_CHANGELOG" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV
          echo "📜 Último changelog preparado."

      # 6️⃣ Envia mensagem formatada com embed estilizada (Lyra Style)
      - name: Enviar notificação ao Discord
        if: env.SKIP != 'true'
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        with:
          args: |
            {
              "username": "The Bard",
              "avatar_url": "https://raw.githubusercontent.com/Leosdc/lyra-the-wise/main/assets/lyra_avatar.png",
              "embeds": [{
                "title": "🧙 Nova versão `${{ env.VERSION }}` publicada automaticamente!",
                "color": 11259375,
                "fields": [
                  {
                    "name": "📦 Repositório",
                    "value": "[`${{ github.repository }}`](https://github.com/${{ github.repository }})",
                    "inline": true
                  },
                  {
                    "name": "👤 Autor",
                    "value": "${{ github.actor }}",
                    "inline": true
                  },
                  {
                    "name": "🕐 Data",
                    "value": "${{ github.event.head_commit.timestamp }}",
                    "inline": false
                  },
                  {
                    "name": "🪶 Changelog",
                    "value": "```md\n${{ env.LATEST_CHANGELOG }}\n```"
                  }
                ],
                "thumbnail": {
                  "url": "https://raw.githubusercontent.com/Leosdc/lyra-the-wise/main/assets/lyra_icon.png"
                },
                "footer": {
                  "text": "Lyra the Wise • Build automático do GitHub Actions"
                }
              }]
            }

      # 7️⃣ Confirmação final no log
      - name: Confirmar envio
        if: env.SKIP != 'true'
        run: echo "✅ Versão $VERSION publicada, tag enviada e notificação entregue com sucesso!"
