name: Discord Notify
on:
  push:
    branches:
      - main
      - master

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Notification
        uses: Ilshidur/action-discord@v2
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          message: |
            📦 **Novo push no repositório `${{ github.repository }}`**
            👤 Autor: `${{ github.actor }}`
            📝 Mensagem: ${{ github.event.head_commit.message || 'Sem mensagem de commit' }}
            🌿 Branch: `${{ github.ref_name }}`
            🔗 [Ver repositório](https://github.com/${{ github.repository }}/commits/${{ github.ref_name }})
